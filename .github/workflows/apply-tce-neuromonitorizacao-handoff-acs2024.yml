name: Apply TCE neuromonitoring handoff ACS 2024 checkpoint

on:
  push:
    branches: [emergencias-2-ui-core]
    paths:
      - scripts/ajusta-tce-neuromonitorizacao-handoff-acs2024.cjs
      - scripts/valida-tce-neuromonitorizacao-handoff-acs2024.cjs
      - .github/workflows/apply-tce-neuromonitorizacao-handoff-acs2024.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: emergencias-2-ui-core
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Apply neuromonitoring handoff checkpoint
        run: node scripts/ajusta-tce-neuromonitorizacao-handoff-acs2024.cjs
      - name: Focused neuromonitoring handoff validation
        run: node scripts/valida-tce-neuromonitorizacao-handoff-acs2024.cjs
      - name: Existing TCE regression
        run: npm run test:tce
      - name: Emergencias 2 architecture suite
        run: npm run test:emergencias-2
      - name: Scoped TypeScript compile
        run: npx tsc --module node16 --target es2020 --esModuleInterop --moduleResolution node16 --skipLibCheck --outDir /tmp/tce-handoff tce-decision-tree.ts lib/alvos-tce.ts lib/pas-no-tce.ts lib/clinical-transition-contracts.ts lib/clinical-transitions.ts
      - name: Validate scope
        run: |
          git diff --check
          changed="$(git diff --name-only | sort)"
          echo "$changed"
          expected="$(printf '%s\n' lib/clinical-transition-contracts.ts lib/i18n/modules/tce.ts tce-decision-tree.ts | sort)"
          test "$changed" = "$expected"
      - name: Commit validated neuromonitoring handoff checkpoint
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/clinical-transition-contracts.ts lib/i18n/modules/tce.ts tce-decision-tree.ts
          git commit -m "fix(tce): harden neuromonitoring and neurosurgical handoff"
          git pull --rebase origin emergencias-2-ui-core
          git push origin HEAD:emergencias-2-ui-core
