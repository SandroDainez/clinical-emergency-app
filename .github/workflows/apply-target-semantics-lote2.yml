name: Archived target semantics lote 2 check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "Target semantics lote 2 was applied successfully; this workflow is read-only and retained only as an audit marker."
