name: Archived shared shell target navigation migration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "Shared shell target navigation migration was applied successfully; this workflow is read-only and retained only as an audit marker."
