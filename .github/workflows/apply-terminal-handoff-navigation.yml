name: Archived terminal handoff navigation check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "Terminal handoff navigation integration was applied successfully; this workflow is read-only and retained only as an audit marker."
