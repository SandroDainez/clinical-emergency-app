name: Archived ISR awake airway rescue and post-intubation 2026

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "ISR awake airway rescue and post-intubation migration applied and validated."
