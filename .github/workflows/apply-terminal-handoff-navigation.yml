name: Archived sedation dose-limit semantics

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "Sedation dose-limit update applied, validated and archived; retained only as an audit marker."
