name: Apply VM obstructive strategy 2026

on:
  push:
    branches:
      - emergencias-2-ui-core

permissions:
  contents: write

jobs:
  migrate-and-validate:
    if: ${{ !contains(github.event.head_commit.message, '[vm-obstructive-2026-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: emergencias-2-ui-core
          fetch-depth: 0

      - name: Apply VM obstructive migration
        run: |
          node scripts/aplica-vm-obstrutivo-2026.cjs
          node scripts/ajusta-vm-obstrutivo-i18n-2026.cjs

      - name: Validate ventilation module
        run: npm run test:vm

      - name: Validate VM obstructive strategy 2026
        run: node scripts/valida-vm-obstrutivo-2026.cjs

      - name: Validate translation consistency
        run: npm run test:traducao-composta

      - name: Validate Emergencias 2 structural suite
        run: npm run test:emergencias-2

      - name: Commit validated migration
        run: |
          if git diff --quiet; then
            echo "No VM obstructive changes needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ventilation-decision-tree.ts lib/i18n/modules/ventilacao.ts
          git commit -m "align obstructive VM auto-PEEP permissive hypercapnia and adjuncts [vm-obstructive-2026-applied]"
          git push origin HEAD:emergencias-2-ui-core
