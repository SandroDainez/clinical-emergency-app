name: Archived sedation BNM UI semantics

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "Sedation BNM UI semantics migration applied and validated."
