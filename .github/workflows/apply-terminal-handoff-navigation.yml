name: Archived compact sedation calculation flow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "Compact sedation calculation flow migration applied and validated."
