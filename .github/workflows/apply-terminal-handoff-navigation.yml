name: Archived SCCM 2026 NMBA semantics

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "SCCM 2026 NMBA update applied and validated."
