name: Archived canonical succinylcholine obesity semantics

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "Canonical succinylcholine obesity/weight update applied and validated."
