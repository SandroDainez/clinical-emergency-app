name: Archived mechanical ventilation initial layer 2026

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "Mechanical ventilation initial-layer migration applied and validated."
