name: Archived ISR NMBA and ventilation coherence

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "ISR NMBA and peri-induction ventilation migration applied and validated."
