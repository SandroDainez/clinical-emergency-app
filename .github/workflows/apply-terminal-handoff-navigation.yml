name: Archived fentanyl context-sensitive semantics

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "Fentanyl context-sensitive semantics update applied, validated and archived; retained only as an audit marker."
