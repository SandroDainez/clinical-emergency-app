name: Apply VM obesity acidosis neuromuscular 2026

on:
  push:
    branches:
      - emergencias-2-ui-core

permissions:
  contents: write

jobs:
  migrate-and-validate:
    if: ${{ !contains(github.event.head_commit.message, '[vm-obesity-acidosis-nmd-2026-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: emergencias-2-ui-core
          fetch-depth: 0

      - name: Apply VM obesity/acidosis/NMD migration
        run: |
          node scripts/aplica-vm-obesidade-acidose-neuromuscular-2026.cjs
          node scripts/ajusta-vm-obesidade-acidose-neuromuscular-i18n-2026.cjs

      - name: Validate ventilation module
        run: npm run test:vm

      - name: Validate VM obesity/acidosis/NMD 2026
        run: node scripts/valida-vm-obesidade-acidose-neuromuscular-2026.cjs

      - name: Validate translation consistency
        run: npm run test:traducao-composta

      - name: Validate Emergencias 2 structural suite
        run: npm run test:emergencias-2

      - name: Commit validated migration
        run: |
          if git diff --quiet; then
            echo "No VM obesity/acidosis/NMD changes needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ventilation-decision-tree.ts lib/i18n/modules/ventilacao.ts
          git commit -m "align obesity metabolic acidosis and neuromuscular ventilation [vm-obesity-acidosis-nmd-2026-applied]"
          git push origin HEAD:emergencias-2-ui-core
