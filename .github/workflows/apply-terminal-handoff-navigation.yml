name: Archived remifentanil ICU and morphine renal semantics

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "Remifentanil ICU path and morphine renal semantics applied, validated and archived."
