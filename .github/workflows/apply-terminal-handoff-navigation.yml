name: Archived succinylcholine hyperkalemia semantics

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "Succinylcholine hyperkalemia semantics update applied, validated and archived; retained only as an audit marker."
