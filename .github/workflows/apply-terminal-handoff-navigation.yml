name: Apply compact sedation calculation flow

on:
  push:
    branches:
      - emergencias-2-ui-core

permissions:
  contents: write

jobs:
  migrate-and-validate:
    if: ${{ !contains(github.event.head_commit.message, '[sedation-compact-flow-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: emergencias-2-ui-core
          fetch-depth: 0

      - name: Apply compact sedation calculation flow migration
        run: node scripts/aplica-sedacao-calculo-compacto.cjs

      - name: Validate sedation module
        run: npm run test:sedacao

      - name: Validate compact sedation calculation flow
        run: node scripts/valida-sedacao-calculo-compacto.cjs

      - name: Validate sedation UI hierarchy
        run: node scripts/valida-sedacao-ui-hierarquia.cjs

      - name: Validate translation consistency
        run: npm run test:traducao-composta

      - name: Validate Emergencias 2 structural suite
        run: npm run test:emergencias-2

      - name: Commit validated migration
        run: |
          if git diff --quiet; then
            echo "No compact sedation calculation changes needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add components/protocol-screen/sedation-calculator-screen.tsx lib/i18n/modules/sedacao.ts
          git commit -m "compact sedation dilution and bolus detail flow [sedation-compact-flow-applied]"
          git push origin HEAD:emergencias-2-ui-core
