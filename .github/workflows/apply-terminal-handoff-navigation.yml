name: Archived VM final safety cleanup 2026

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "VM final safety cleanup 2026 migration applied and validated."
