name: Archived etomidate safety semantics

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived-check:
    runs-on: ubuntu-latest
    steps:
      - name: Archived migration notice
        run: echo "Etomidate safety update applied, validated and archived; retained only as an audit marker."
